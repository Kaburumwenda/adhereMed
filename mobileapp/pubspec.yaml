name: adheremed_mobile
description: "AdhereMed Pharmacy Mobile App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  go_router: ^14.8.1
  flutter_secure_storage: ^9.2.4
  shared_preferences: ^2.3.5
  intl: ^0.20.2
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.17
  url_launcher: ^6.3.1
  share_plus: ^10.1.4
  pull_to_refresh_flutter3: ^2.0.2
  timeago: ^3.7.0
  flutter_animate: ^4.5.2
  pdf: ^3.12.0
  printing: ^5.14.3
  google_maps_flutter: ^2.14.2
  geolocator: ^14.0.2
  image_picker: ^1.2.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # run: flutter clean && flutter pub get && flutter pub run flutter_native_splash:create
#   flutter_native_splash: ^2.2.17

# flutter_native_splash:
#   color: "#000000"
#   image: assets/images/logo.png
#   android: true
#   ios: true 

flutter:
  uses-material-design: true
  assets:
    - assets/images/
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

    #### commands
# flutter pub global run rename setBundleId --targets ios,android --value "com.netx.movies"
#flutter pub global run rename setAppName --targets ios,android --value "Netx Movies"