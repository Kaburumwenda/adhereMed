// Theme init is handled directly inside `plugins/vuetify.js` (the Pinia store
// is wired into the Vuetify instance there). This file is intentionally a
// no-op so we don't double-apply the theme.
export default defineNuxtPlugin(() => {})
